# Обзор проекта Metaluck — 26 августа 2026

## Что это

Telegram Mini App + web: кейсы, daily, колёса, мини-игры (Coinflip, Blackjack, Mine Rush, Arena, Aviator). Client React+Vite on Vercel, API Fastify on Railway, data Supabase. Repo: Brokiesmen/Metaluck. Bot username from live API: metaluck_bot.

## Живые URL

- Frontend (tested): https://metaluck-eight.vercel.app/ (HTTP 200)
- Also 200: https://metaluck.vercel.app (different/smaller build — confusion risk)
- API: https://metaluck-api-production.up.railway.app — /api/auth/config 200, /api/cases Unauthorized, root 404
- Auth config from live API: telegramBot metaluck_bot; google/telegram/ton/evm/walletConnect all ready true; webAppUrl https://metaluck-eight.vercel.app; miniAppPath app
- Latest Railway SUCCESS deploy: 2026-08-07 (commit 6e5ceb6, TON/WalletConnect login). Frontend production later includes Phaser Aviator rewrite — API may lag the client.

## Тест 26.08.2026 (web, без авторизации)

Only the login screen is reachable. Four buttons: Telegram, Google, TON Connect, WalletConnect. TON Connect modal opens (Tonkeeper etc.); did not sign. Escape left a half-dismissed overlay. Routes /cases /games /profile /daily /app /demo all show the same login gate. Demo localStorage flag metaluck_demo_mode=1 does not bypass login. Prod API has devLoginReady false so the in-bundle "Локальный вход" is hidden. No console errors on load. Login copy is hard-coded Russian; i18n exists in bundle but not on the gate. tonconnect-manifest.json terms/privacy URLs point at homepage. Viewport user-scalable=no. Vercel origin /api/* 404s (no proxy); API is cross-origin Railway.

## Аналитика

- No Sentry project in org 12-d7 (zero projects).
- No Sentry/PostHog/analytics SDK in client or server package.json.
- RevenueCat account has a generic project created 2026-08-26 (projd33173e4 / project-VPHb1Idb) — not wired to this app.
- No product analytics, funnels, or error tracking in production.

## Код (коротко)

Phaser catalog currently wires only aviator. Other games still legacy React. Desktop web uses LoginScreen + DesktopShell; Telegram uses TelegramShell.

## Мнение: чего не хватает (для следующего раза)

Priority order:

1. Enable a safe prod demo/guest or reviewer login so the app can be tested past the gate without a real wallet/Telegram.
2. Public ToS + privacy pages; fix TON Connect manifest URLs. 18+ / responsible-gambling basics if this is real-money.
3. Wire Sentry (errors) and a product analytics tool (e.g. PostHog) — currently blind.
4. Language selector on login; don't ignore browser locale.
5. Landing value before auth (what the app is, game list) — four login methods with no explanation.
6. Align Railway API deploy with latest Vercel/Phaser client.
7. Fix TON modal Escape dismiss. Stop leaking nginx/port-3001 debug strings to users.
8. Decide canonical frontend URL (metaluck-eight vs metaluck.vercel.app).
9. RevenueCat only if native iOS/Android store subs are actually planned; not needed for Telegram Stars alone.

## Следующий шаг

To review cases/games/profile: either turn on a non-production reviewer login, or have Mark sign in on the live site. Do not spend, merge, or deploy from this notes-only change.
